<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Person - <%= data[0].id %></title>
    <link rel="stylesheet" href="/person.css">
    <link rel="stylesheet" href="/fonts.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500;1,700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Arsenal+SC:ital,wght@0,400;0,700;1,400;1,700&family=Oswald:wght@200..700&family=Playwrite+CU:wght@100..400&display=swap" rel="stylesheet">

</head>
<body>
    <div class="block">
        <div class="profile">
            <h1><%= data[0].id %></h1>
            <h2><%= data[0].firstName %><%= " " %><%= data[0].midName %><%= " " %><%= data[0].lastName %></h2>
        </div>
        <div class="art">
            <h2>Written Articles</h2>
            <% data.slice(1, data.length).forEach(article=>{ %>
                <% if (article.ispublic === 1) { %>
                    <div class="article">
                        <form action="/readme/<%= article.articleId %>" method="get">
                            <button type="submit">
                                <h2><%= article.title %></h2>
                                <h3><%= article.articleId %></h3>
                            </button>
                        </form>
                    </div>
                <% }%>
            <% }) %>
        </div>
    </div>
</body>
</html>